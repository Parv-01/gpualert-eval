__pycache__/
*.pyc
.venv/
venv/
.env
.DS_Store
*.egg-info/
.ipynb_checkpoints/

# corpus + results are regenerated by `make all`; a sampled copy is committed,
# but local full runs can get large. Keep the big intermediate stuff out.
corpus/injected/*.large.log
results/*.tmp
scratch/
.pytest_cache/
